services:
  app:
    build: .
    environment:
      MAX_UPLOAD_MB: 100
    volumes:
      - ./src:/app/src
    ports:
      - "8015:8000"

  test:
    build: .
    command: python -m unittest discover -s tests -v
    profiles: ["test"]
